[project]
name = "agentic-ai-langchain"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.2",
    "openai>=3.5.0",
    "pandas>=3.0.5",
    "python-dotenv>=1.2.3",
    "yfinance>=1.7.0",
]

[project.scripts]
agentic-ai-langchain = "agentic_ai_langchain:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
